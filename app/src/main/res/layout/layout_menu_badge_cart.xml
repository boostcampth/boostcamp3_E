<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:paddingRight="16dp"
    android:layout_height="40dp"
    android:clipToPadding="false"
    android:focusable="true">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/iv_cart_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:srcCompat="@drawable/btn_shopping_cart"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/cart_badge"
        android:layout_width="15dp"
        android:layout_height="15dp"
        app:layout_constraintTop_toTopOf="@id/cart_badge"
        app:layout_constraintBottom_toTopOf="@id/cart_badge"
        android:background="@drawable/ic_badge_bacground"
        android:textColor="@android:color/white"
        app:layout_constraintRight_toRightOf="@id/iv_cart_img"
        app:layout_constraintLeft_toRightOf="@id/iv_cart_img"
        android:text="99"
        android:gravity="center"
        android:textSize="8sp"/>

</androidx.constraintlayout.widget.ConstraintLayout>